/**
 * useAuth.ts
 * Redirige al contexto global de autenticación para evitar estados duplicados.
 */

export { useAuth } from '@/app/providers/AuthContext';
export type { UserProfile } from '@/app/providers/AuthContext';
